<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
  <meta http-equiv="Pragma" content="no-cache">
  <meta http-equiv="Expires" content="0">
  <title>Setup Password - Healora</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  
  <link rel="stylesheet" href="/css/reset-password.css">
</head>
<body>
  <div class="setup-container">
    <h2>Setup Your Password</h2>
    <p class="subtitle">Since you signed up with Google, please set a password for direct login</p>

    <div id="serverError"></div>
    <div id="successMessage"></div>

    <form id="setupPasswordForm" novalidate>
      <div class="password-wrapper">
        <input type="password" name="password" id="password" class="form-control" placeholder="New Password (min 6 characters)" required>
        <i class="fas fa-eye toggle-password" id="togglePassword"></i>
      </div>
      <div id="errorPassword"></div>

      <div class="password-wrapper">
        <input type="password" name="confirmPassword" id="confirmPassword" class="form-control" placeholder="Confirm Password" required>
        <i class="fas fa-eye toggle-password" id="toggleConfirmPassword"></i>
      </div>
      <div id="errorConfirmPassword"></div>

      <button type="submit" class="btn-submit">Set Password</button>
      <p style="margin-top: 15px; color: #718096; font-size: 14px;">
        <a href="/patient/dashboard" style="color: #667eea;">Skip for now</a>
      </p>
    </form>
  </div>

  <script src="/js/formValidation.js"></script>
  <script>
    FormValidator.handleFormSubmit(
      'setupPasswordForm',
      '/patient/setup-password',
      'resetPassword', 
      {
        onSuccess: (result) => {
          document.getElementById('successMessage').style.display = 'block';
          document.getElementById('successMessage').textContent = result.message;
          setTimeout(() => {
            window.location.href = result.redirect;
          }, 1500);
        }
      }
    );

    FormValidator.setupPasswordToggle('password', 'togglePassword');
    FormValidator.setupPasswordToggle('confirmPassword', 'toggleConfirmPassword');
  </script>
</body>
</html>