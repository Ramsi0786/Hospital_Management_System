<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Healora login page - choose your role to access personalized dashboard" />
  <title><%= title %> | Healora</title>

  <!-- Favicon -->
  <link rel="icon" href="/images/Logo-browser.jpg" />

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet" />

  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/style.css" />
  
  <style>
    /* Section background */
    .role-section {
      background-color: #f8f9fa;
    }

    /* Left image */
    .role-section img.side-image {
      height: 100vh;
      object-fit: cover;
    }

    /* Right content */
    .role-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background-color: #fff;
      text-align: center;
      padding: 20px;
    }

    /* Logo styling */
    .role-content img.logo {
      width: 70px;                /* smaller, balanced size */
      height: auto;
      margin-bottom: 20px;
      display: block;
    }

    /* Rectangular login boxes */
    .login-box {
      border: 2px solid #0d6efd;
      border-radius: 12px;
      padding: 15px;
      width: 230px;
      text-align: center;
      font-weight: 600;
      color: #0d6efd;
      transition: all 0.3s ease;
      cursor: pointer;
      text-decoration: none;
      background-color: #fff;
    }

    .login-box:hover {
      background-color: #0d6efd;
      color: #fff;
      transform: translateY(-4px);
      box-shadow: 0 6px 15px rgba(13, 110, 253, 0.3);
    }

    /* Spacing between boxes */
    .login-options {
      display: flex;
      flex-direction: column;
      gap: 15px;
      margin-top: 25px;
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
      .role-section img.side-image {
        height: 250px;
      }
      .role-content {
        height: auto;
        padding: 60px 0;
      }
    }
  </style>
</head>
<body>

  <section class="container-fluid px-0 role-section">
    <div class="row g-0">

      <!-- Left: Image -->
      <div class="col-md-6">
        <img src="/images/try-login.jpg" class="side-image w-100" alt="Healora login illustration" loading="lazy" />
      </div>

      <!-- Right: Role Selection -->
      <div class="col-md-6 role-content">
        <img src="/images/logo.jpg" class="logo mx-auto" alt="Healora Logo" loading="lazy" />
        <h3 class="fw-bold text-primary mb-3">Choose Your Login</h3>
        <p class="text-muted mb-4">Access your personalized Healora dashboard</p>

        <div class="login-options">
          <a href="/patient/login" class="login-box">Patient Login</a>
          <a href="/doctor/login" class="login-box">Doctor Login</a>
          <a href="/admin/login" class="login-box">Admin Login</a>
        </div>
      </div>

    </div>
  </section>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
